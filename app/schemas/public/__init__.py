"""Wire formats for the audience-facing (`routers/public/`) endpoints.

These are not tool schemas: nothing here belongs to ZAP or JMeter. They are the
shapes the server presents to an operator, assembled from tool-owned data that
keeps living in ``schemas/tools/<tool>/``.
"""
